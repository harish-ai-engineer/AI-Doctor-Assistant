[project]
name = "medtrace-api"
version = "1.0.0"
description = "Production-oriented AI Doctor Assistant for learning AgentGuard"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
